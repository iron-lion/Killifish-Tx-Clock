name: killifish-tx-clock
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - statsmodels
  - matplotlib
  - seaborn
  - openpyxl
  - pip
  - pip:
      - pydeseq2
      - inmoose
      - pybiomart      # optional: only needed to rebuild gene ID mapping
